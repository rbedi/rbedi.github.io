# macOS
.DS_Store

# Claude Code / local tooling (dev-server config, local settings)
.claude/

# Editor scratch
*.swp

# Raw source drafts — kept local, not published (may contain comments / tracked changes)
RAW_posts/

# Word lock/temp files
~$*
